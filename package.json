{
  "name": "skylink-corridor-website",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "netlify dev",
    "check": "node --check app.js && node --check netlify/functions/send-quote.mjs"
  },
  "dependencies": {
    "nodemailer": "^9.0.3"
  },
  "devDependencies": {
    "netlify-cli": "^23.7.3"
  }
}

{
  "name": "skylink-corridor-website",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check app.js && node --check netlify/functions/send-quote.mjs"
  },
  "dependencies": {
    "nodemailer": "^9.1.1"
  },
  "devDependencies": {
    "netlify-cli": "^23.7.3"
  }
}
